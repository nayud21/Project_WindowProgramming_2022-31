<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SellingTree.NavigationBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SellingTree"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <local:IntToVisibilityIConverter x:Key="VisibilityConverter"/>
    </Page.Resources>
    <Grid Background="Green">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Column="0">
            <Button x:ConnectionId='3' x:Name="MainPageButton" Margin="10,0,0,0"                             >
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE80F;" />
                    <TextBlock Margin="10,0,0,0" Text="Home"/>
                </StackPanel>
            </Button>
            <Button x:ConnectionId='4' x:Name="ShopListButton" Margin="10,0,0,0"                               >
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE7BF;" />
                    <TextBlock Margin="0,0,0,0" Text="Giỏ hàng"/>
                    <InfoBadge Background="Red"
                               Value="{Binding Count}" HorizontalAlignment="Right" VerticalAlignment="Top"
                               Visibility="{Binding Count, Converter={StaticResource VisibilityConverter}}">
                        <InfoBadge.IconSource>
                            <FontIconSource FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xF13C;" />
                        </InfoBadge.IconSource>
                    </InfoBadge>

                </StackPanel>
            </Button>
            <Button x:ConnectionId='5' x:Name="blogButton"                          Margin="10,0,0,0">Blog</Button>
            <Button x:ConnectionId='6' x:Name="chatButton"                          Margin="10,0,0,0">Chat</Button>
            <Button x:ConnectionId='7' x:Name="collectionButton"                                Margin="10,0,0,0">Collection</Button>
        </StackPanel>
        <Button x:ConnectionId='2' x:Name="accountButton"                             Margin="10,0,10,0" Grid.Column="1" >Account</Button>
    </Grid>
</Page>

