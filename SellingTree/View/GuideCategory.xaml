<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SellingTree.View.GuideCategory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SellingTree.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Name="backButton" Margin="10,0,0,0" Click="BackButton_Click">
                <FontIcon Glyph="&#xE72B;"/>
            </Button>
            <Button Name="reloadButton" Margin="10,0,0,0" Click="ReloadButton_Click">
                <FontIcon Glyph="&#xE72C;"/>
            </Button>
        </StackPanel>
        <WebView2 Grid.Row="1" Name="webView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

    </Grid>
</Page>
